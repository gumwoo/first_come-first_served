# IMP-XXX · <개선 제목>

- 슬라이스: `<S0x>`
- 날짜: <YYYY-MM-DD>
- 유형: 정량(수치) | 정성(동작/설계) — 정성이면 3·7단계는 수치 대신 재현 시나리오로
- 관련 커밋/PR: <before 커밋> → <after 커밋>
- 벤치 파일: `benchmarks/<name>-before.json` → `benchmarks/<name>-after.json`

> 흐름: **문제 정의 → 증상(수치) → 가설 → 도구로 검증 → 해결 → 재측정 → 한계**.
> 원칙: 해결책(캐시/락/비동기)을 먼저 넣지 말고, **어디서 시간·오류가 나는지 먼저 나눠서 측정**한다.

## 1. 상황 (Context)
<어떤 기능에서, 왜 이 문제를 보게 됐는가. 어떤 계기(부하테스트/리뷰/장애)로 관찰됐는가>

## 2. 문제 정의 + 분류
<한 문장으로 "무슨 문제"인지. "서버가 느리다" 금지 — 어느 계층 문제인지 지목>

- 계층 분류(해당 항목): DB / 애플리케이션 코드 / 외부 API / 네트워크 /
  스레드·커넥션 풀 / 캐시 정합성 / 동시성 / 프론트 렌더링
- 왜 그 계층인가(1차 근거): <예: 내 쿼리는 빠른데 전체 응답이 느림 → 외부 API 의심>

## 3. 증상 (측정된 증거)
<추측이 아니라 수치. 평균만 말고 p95/p99·실패율까지>

- 측정 방법: <k6 시나리오(VU/duration) / 동시성 테스트(스레드 수) / EXPLAIN / JFR / 구간 로그>
- before 수치:
  | 지표 | 값 |
  |------|----|
  | 예: 초과판매 | 37건 |
  | 예: p95 / p99 | 850ms / 1,900ms |
  | 예: 쿼리 수(N+1) | 51 |
  | 예: EXPLAIN rows | 250,000 (풀스캔) |

## 4. 가설 (원인 추측 — 검증 전)
<증상을 보고 가능한 원인을 좁힌다. 아직 해결책 아님. 여러 개면 나열하고 우선순위>
- 가설 1: <예: WHERE/ORDER BY에 맞는 인덱스가 없어 풀스캔 + filesort>
- 가설 2: <예: OFFSET 페이징으로 뒤 페이지가 느림>

## 5. 검증 (도구로 확인 → 확정된 원인)
<가설을 어떤 도구로 확인했는지, 결과가 무엇이었는지. 여기서 원인을 1개로 확정>
- 사용 도구: <EXPLAIN(ANALYZE) / k6 / JFR / Thread Dump / Hibernate statistics / 구간별 latency 로그>
- 확인 결과: <예: EXPLAIN에서 type=ALL, key=NULL, Using filesort → 인덱스 미사용 확정>

## 6. 조치 (해결)
<확정된 원인에 맞는 해결책만. 문제 유형에 맞게 선택한 이유 포함. 핵심 코드/설정 변경 요약>

## 7. 결과 (재측정 — before와 동일 조건)
- 측정 방법: <2·3단계와 동일 조건 명시(같은 VU/데이터량/워밍업)>
- after 수치 + 변화율:
  | 지표 | before | after | 변화 |
  |------|--------|-------|------|
  | 예: 초과판매 | 37건 | 0건 | -100% |
  | 예: p95 | 850ms | 210ms | -75% |
  | 예: TPS | 120 | 1,400 | +11.6x |

## 8. 트레이드오프 / 한계 / 다음 개선
<개선의 비용(쓰기 성능·메모리·복잡도), 남은 한계, 분산환경 고려, 다음에 개선할 것>

## 9. 배운 점 (면접 답변용 한 줄)
<이 개선을 "증상→가설→검증→해결→한계" 한 문장으로. 도구 이름이 아니라 사고 과정이 드러나게>
