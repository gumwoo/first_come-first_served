# <ID> · <화면명>

- ref: `<참조 이미지 경로>`
- route: `<경로>`
- slice: `<S0x>`
- 참조: common/design-system.md, common/layout.md, common/api-contract.md

## 목적
<이 화면이 존재하는 이유 / 진입 맥락>

## 상태(states)
- `<state>` — <조건>

## 트리거 / 진입
- <어디서 어떤 조건으로 진입하는가>

## 사용 API
- `<METHOD /path>` — <용도>

## 화면 요소 (DoD 체크리스트)
- [ ] <필수 UI 요소>

## 연결(이탈 경로)
- <버튼/링크> → <목적지 화면>
